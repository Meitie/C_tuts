int functionsTut(){



    return 0;
}
/*
you want to prototype every function before main so that C knows that it exists;
do so by copying the first line so for this = void printSomething();
*/


//void = make function doesnt recieve anything in return
void printSomething() {
    printf("durr im a function\n");

    //functions always need a return, done with function go back something else
    return;
}
